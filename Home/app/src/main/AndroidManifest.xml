<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="my.benjamin.com.home">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".info_tech"></activity>
        <activity android:name=".com_skills" />
        <activity android:name=".computer_maths" />
        <activity android:name=".probability" />
        <activity android:name=".intro_cs" />
        <activity android:name=".hardware" />
        <activity android:name=".networking" />
        <activity android:name=".programming" />
        <activity android:name=".about" />
        <activity android:name=".notes" />
        <activity android:name=".questions" />
        <activity android:name=".HELP" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>